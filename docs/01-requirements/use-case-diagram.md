# Use Case диаграмма

```mermaid
graph LR
    Student((Студент))
    Manager((Менеджер группы))

    UC1[UC-01 Зарегистрироваться]
    UC2[UC-02 Войти в систему]
    UC3[UC-03 Просмотреть расписание]
    UC4[UC-04 Управлять задачами]
    UC5[UC-05 Управлять заметками]
    UC6[UC-06 Привязать задачу/заметку к занятию]
    UC7[UC-07 Синхронизировать расписание группы]
    UC8[UC-08 Подключить личный аккаунт ИС вуза]
    UC9[UC-09 Просмотреть/отключить привязку личного аккаунта]

    Student --> UC1
    Student --> UC2
    Student --> UC3
    Student --> UC4
    Student --> UC5
    Student --> UC6
    Student --> UC8
    Student --> UC9
    Manager --> UC7

    UC6 -.включает.-> UC3
    UC4 -.включает.-> UC6
    UC5 -.включает.-> UC6
```

## Актёры

| Актёр | Описание |
|---|---|
| Студент (`ROLE_USER`) | Основной пользователь — ведёт задачи/заметки, просматривает расписание своей группы |
| Менеджер группы (`ROLE_MANAGER`) | Расширенная роль — вызывает синхронизацию расписания группы с парсером |

Роль `ROLE_ADMIN` зарезервирована (см. модель ролей в [02-architecture/pcmef-diagram.md](../02-architecture/pcmef-diagram.md)), отдельных UC для неё в текущей итерации не реализовано — см. отложенный пункт «админ-панель» в [10-final-report/summary.md](../10-final-report/summary.md).
